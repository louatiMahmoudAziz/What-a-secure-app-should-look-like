# Cryptographic Design

> Stub. Must cover: scrypt parameters and why scrypt for payloads vs Argon2id for
> passwords; AES-256-GCM and nonce strategy; envelope format with the two-region
> layout (sequential header carrying salt/params, permuted ciphertext region);
> fail-safe generic error behavior.
