# Known Limitations

> Stub. Must include: no password reset flow, no email verification, LSB
> detectability, IP hashing is pseudonymization not anonymization, refresh token
> returned in response body (documented tradeoff).
